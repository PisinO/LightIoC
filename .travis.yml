
language: swift
osx_image: xcode10.6

env:
  matrix:
   - DESTINATION="platform=iOS Simulator,OS=13.6,name=iPhone 11"

before_script: cd Example

script:
   - xcodebuild clean test -workspace LightIoC.xcworkspace -scheme LightIoC-Example -destination "${destination}" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO bash <(curl -s https://codecov.io/bash)

after_success:
  - bash <(curl -s https://codecov.io/bash)
